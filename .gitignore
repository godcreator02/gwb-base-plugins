node_modules/
dist/
*.log

# python 那半的运行时产物：venv 由守卫现建（装进 node_modules 之后也一样），
# 里头几十 MB 加一堆写死的绝对路径——进不得 git，也不进包
.venv/
__pycache__/
*.pyc

# ZCode 与 .claude/skills 共享用的 junction（本机重建：cmd /c mklink /J .agents\skills <仓根>\.claude\skills）
# git 把 junction 当普通目录递归，不挡住就会把 skills 再复制一份进库
.agents/
