---
title: 定过的事
description: 选型与它们的依据——拍板之后记在这儿
docfirst: none
---

# 定过的事

按 `gwb-decide` 的规矩：选型推完交人拍板，拍板之后记进这页。只记关系**插件**的选型；
关系**规则**的（命名、依赖判据、内核该不该做某事）归内核仓的站。

条目记「定了什么、为什么」；经过、读数与翻案史随轨迹卡走，正本在卡里，这页不抄第二份。
**此刻没有挂着的条目**——仍然成立的规则各自住在对应插件页（一键更新为什么是热重挂、
MCP 工具面为什么收窄、default 端口为什么不退让、装插件时 peer 谁装谁跳过、卸载为什么
先摘条目后卸包，见[插件管理](/docs/plugin-manager)与[mcp](/docs/mcp)），三仓拆分的
判据在轨迹卡 `2026-09-12-three-repos-split`、HTTP 入口接班的判据在轨迹卡
「HTTP 门接班」。
