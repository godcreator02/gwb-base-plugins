---
title: gwb-data 的身份卡
description: gwb-data 的身份卡——是谁、提供什么、靠谁；render 摆成包内 catalog.md
---

# gwb-data

**定位**：插件落盘状态的统一出口——要落盘就用它，不自己拼路径、不碰 fs。

## 提供

`gwbData`：readDoc(doc) / writeDoc(doc, value)（原子落盘），外加 dir（本件那格数据目录）；分家按条目 id 末段，落 `<>`/data/`<>`/

## 依赖



## 命令

命令清单不存卡——**经门现扫**（MCP 门 `search`，`plugin` 用包名筛这一组）。
