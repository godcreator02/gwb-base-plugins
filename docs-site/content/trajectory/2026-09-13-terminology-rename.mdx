---
title: 术语改口：件→插件、井→窗格容器、格→窗格
born-at: 2026-09-13T23:13
type: choice
parent: [2026-09-13-base-plugins]
label: 术语改口
domain: 文档
---

gwb 线全仓文档统一改口，本仓现状区与 outputs 正本同批换：**件→插件**（件仓→插件仓、
基础件→基础插件、无头九件→九个无头插件、枚件→一个插件、窗格件→窗格插件、本件→本插件）、
**井→窗格容器**（well，代码模块 well-hub；空井→空窗格容器）、**格→窗格或一项**（一格→
一个窗格；runtime.json 的那格→那一项）、**口→入口/端口**（一道口→一个入口）、
**刀→第 X 阶段**、**皮→界面外观**、**门→入口**（JSON 门→JSON 入口）。

**历史出清同批做**：「定过的事」页十五段日期叙事删去——桌面分组与多桌面两波、窗格身份、
peer 进 home、一键热更、MCP 工具面收窄、default 端口不退让、样式围栏换代、单进程迁移、
状态格下架、市场并入、多桌面撤回、白名单退场、装卸三改——正本各自住在对应插件页或内核
仓的卡里（单进程迁移见内核仓 `2026-09-09-single-process`、`2026-09-09-plugins-migrate-0-1`）；
三仓拆分史落进本区拆分卡；「plugins 断代改名 plugin-manager」的改名史从 mcp /
plugin-manager 页正文删去（正本在拆分卡）；overview 那段「换过内核、其余页面还没跟上」的
过渡告示随各页跟上而删。墙页教训本体保留、只换术语。

改口判据：单字黑话代称退出文档面，换成正常中文表述；机制词（正本、钉码、账本、轨迹、
条目、外壳、窗格、桌面、热挂载、home）保留。命令名、包名、模块名、代码标识符与 UI 字面
名不动（字面名以引号钉住，如「验收件」；轨迹卡名与日志字面量——「HTTP 门接班」、「HTTP
门就绪」——是身份，不改）。本仓旧卡与更早的会话记录里见到的「件」「井」「格」，分别指
插件、窗格容器、窗格。
