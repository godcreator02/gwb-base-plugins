{
  "name": "gwb-base-plugins",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "tsc --noEmit -p tsconfig.json && pnpm -r --no-bail typecheck",
    "lint": "oxlint . --type-aware --deny-warnings",
    "test": "vitest run",
    "check": "pnpm typecheck && pnpm lint && pnpm test",
    "doc": "pnpm --filter gwb-base-plugins-docs-site exec livedoc check",
    "doc:confirm": "pnpm --filter gwb-base-plugins-docs-site exec livedoc confirm"
  },
  "//devDeps": "仓根只装门禁用的：oxlint、vitest、typescript、@types/node。构建线不再是仓里共享的一份——每个件自己装 esbuild 与 @tailwindcss/cli，样式表由件自己那份 src/client/styles.css 用 Tailwind 前缀直出（2026-09-09 起，tools/build-styles.ts 那条选择器 scope 的线随之删除）",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "oxlint": "1.81.0",
    "oxlint-tsgolint": "7.0.2001",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
