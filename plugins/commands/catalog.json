{
  "plugin": "gwb-commands",
  "summary": "命令总线——一切能力的单一交互口，界面与 agent 走同一张表。生态的底座件。",
  "provides": [
    {
      "service": "gwbCommands",
      "shape": "register(def, handler)（description 必写参数形状，无参数明写）/ run(name, args) / list()；回执统一 GwbResult 信封 {ok,data}|{ok:false,error}"
    }
  ],
  "depends": {
    "hard": [],
    "local": [
      "gwbSkills"
    ]
  }
}
