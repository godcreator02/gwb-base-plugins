# gwb-commands

**定位**：命令总线——一切能力的单一交互口，界面与 agent 走同一张表。生态的底座件之一。

## 提供

`gwbCommands`：register(def, handler)（description 必写参数形状，无参数明写）/ run(name, args) / list()；回执统一 GwbResult 信封

## 依赖

`gwbSkills`（局部注入）

## 命令

命令清单不存卡——**现扫 `/surface`**（`?plugin=gwb-commands` 取这一组的形状）。
