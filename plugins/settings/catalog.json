{
  "plugin": "gwb-settings",
  "summary": "设置的唯一正路——插件的配置一律 define 出来，人在界面改、agent 读写；不吃 cordis.yml 的 config。",
  "provides": [
    {
      "service": "gwbSettings",
      "shape": "define({ key, title, type, default?, shared? }) / get(key) / set(key, value)；类型 string|number|boolean|secret；值跟条目走，shared 才落公共区"
    }
  ],
  "depends": {
    "hard": [],
    "local": [
      "gwbCommands",
      "gwbSkills"
    ]
  }
}
