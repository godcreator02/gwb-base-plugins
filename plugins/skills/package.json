{
  "name": "@godcreator/gwb-skills",
  "version": "0.4.0",
  "author": {
    "name": "godcreator"
  },
  "description": "skill 服务：谁有说明书谁自己挂上来，收集与查询。0.3：skill.list / skill.read 摘掉 top 标——MCP 门 0.4 工具面封顶于 surface/run，说明书经 run 按名调。0.4：skill.list/read 随 commands 0.4 拆 description/usage，plugin 改口包名",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "catalog.md",
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "prepublishOnly": "pnpm build"
  },
  "//deps": "三条都是 peer。cordis 与词汇表包同 gwb-data 那两档（Service 基类要 extends，必须是宿主那一份；GwbContext 出现在导出的 constructor 签名里）。gwb-commands 是第三档：这个件只 `import type {}` 引它，但那句副作用类型导入会原样留在 .d.ts 里（它有语义——激活 declare module），消费方解析得到才行",
  "//peerRange": "写 >= 不写 ^：caret 在 0.0.x 上锁死 patch（^0.0.1 只匹配 0.0.1）。下限＝本仓验过的那一版：词汇表包同 dev 钉号，commands 同仓里当前版本",
  "peerDependencies": {
    "@godcreator/gwb-commands": ">=0.4.1",
    "@godcreator/gwb-plugin-api": ">=0.1.1",
    "cordis": "^4.0.0-rc.9"
  },
  "devDependencies": {
    "@godcreator/gwb-commands": "workspace:*",
    "@godcreator/gwb-plugin-api": "0.1.1",
    "cordis": "4.0.0-rc.9",
    "typescript": "^7.0.2"
  }
}
