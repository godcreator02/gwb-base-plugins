packages:
  - plugins/*
  # 共享包：不是 cordis 件，是件的依赖（运行时环境、设计令牌），跟件一起装进 home
  - packages/*
  # 文档站：自己的依赖树，不进根 check
  - docs-site

allowBuilds:
  esbuild: true

minimumReleaseAgeExclude:
  - '@godcreator02/gwb-plugin-api@0.0.1 || 0.0.2 || 0.0.3'
  - '@godcreator02/gwb-cli@0.0.4'
